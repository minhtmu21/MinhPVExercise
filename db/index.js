const express = require('express');
const user_router = express.Router();

let addUser





(async () => {
    const result = await LoginModel.findOne({
        username: 'minh'
    })
    console.log(result)
})

module.exports = {}